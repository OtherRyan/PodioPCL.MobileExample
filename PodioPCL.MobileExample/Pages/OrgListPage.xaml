<?xml version="1.0" encoding="utf-8" ?>
<controls:LoadingContentPage xmlns="http://xamarin.com/schemas/2014/forms"
                             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                             xmlns:controls="clr-namespace:PodioPCL.MobileExample.Controls;assembly=PodioPCL.MobileExample"
                             IsLoading="{Binding IsLoading}"
                             LoadingTitle="{Binding LoadingTitle}"
                             LoadingMessage="{Binding LoadingMessage}"
                             x:Class="PodioPCL.MobileExample.Pages.OrgListPage"
                             Title="Organizations">
  <ListView ItemsSource="{Binding Models}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <ViewCell.View>
            <StackLayout Orientation="Horizontal">
              <StackLayout>
                <Label Text="{Binding OrgId}" />
              </StackLayout>
            </StackLayout>
          </ViewCell.View>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</controls:LoadingContentPage>